# 🌐 Tafsir Editor - Web UI

## Запуск приложения

```bash
# 1. Убедитесь что установлены зависимости
pip3 install -r requirements.txt

# 2. Запустите Streamlit приложение
streamlit run app.py
```

Приложение откроется автоматически в браузере по адресу `http://localhost:8501`

## Возможности Web UI

### 📤 Загрузка документов
- Поддержка `.docx` файлов
- Drag & drop интерфейс

### 📊 Предпросмотр
- **Оригинал:** Красивый preview с цветовой кодировкой блоков
  - 🕌 Аяты: темно-красный, арабский шрифт
  - 📝 Переводы: черный
  - 💬 Комментарии: темно-серый, с отступом слева
  - 📖 Объяснения: темно-зеленый, с отступом слева

### 🤖 AI Редактор
- **Режим:** Корректор (НЕ переписывает!)
- **Кнопка:** "Найти ошибки и исправить"
- **Custom Instruction:** Дополнительные инструкции для AI
- **Лимит блоков:** Для тестирования
- **Dry Run:** Предпросмотр без сохранения

### 💾 Checkpoint System
- ✅ Кэширование результатов
- ✅ Возобновление после прерывания
- ✅ Кнопка "Очистить кэш"

### 📥 Скачивание
- Результат: `*_edited.docx` с Word-level diff
- Формат:
  - 🔴 Зачеркнутый = удален
  - 🟡 Желтый = добавлен
  - ⚫ Обычный = без изменений

## Обновления БД

### Золотой стандарт транслитерации

Правила в `database/schema.py` обновлены согласно инструкции заказчика:

```
tha (ث)     → с̱  (межзубная с)
ha_soft (ح) → х  (русская х!)
zal (ذ)     → з̱  (межзубная з)
sad (ص)     → с  (эмфатическая)
dad (ض)     → д  (эмфатическая)
ta_hard (ط) → т  (эмфатическая)
za (ظ)      → з  (эмфатическая)
ain (ع)     → ʻ  (перевернутый апостроф)
ghain (غ)   → г  (гайн)
ha_hard (ه) → h  (латинская h - КРИТИЧНО!)
waw (و)     → в
hamza (ء)   → ʼ  (апостроф)
```

## Новый тип блоков: EXPLANATION

Автоматически распознает блоки, начинающиеся с:
- "Объяснение:"
- "Толкование:"
- "Тафсир:"

Рендерится с отступом слева (как COMMENTARY) для имитации верстки книги.

## Стек технологий

- **Streamlit** - Web UI framework
- **OpenAI GPT-4o-mini** - AI корректор
- **python-docx** - Word processing
- **difflib** - Word-level diff
- **Supabase** - Database

## Production Ready Features

✅ Word-level diff (хирургический!)
✅ Checkpoint system (resumable processing)
✅ Retry mechanism (exponential backoff)
✅ Graceful shutdown (Ctrl+C safe)
✅ Cache management (load/clear)
✅ Beautiful Unicode brackets ﴿﴾
✅ Web UI (Streamlit)
✅ Золотой стандарт транслитерации
